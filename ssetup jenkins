docker run -d \
  -p 8080:8080 -p 50000:50000 \
  -v jenkins_home:/var/jenkins_home \
  jenkins/jenkins:lts


  Get admin password:

docker exec $(docker ps -qf "ancestor=jenkins/jenkins:lts") \
  cat /var/jenkins_home/secrets/initialAdminPassword

  Open Jenkins:
👉 http://localhost:8080

Install recommended plugins.

